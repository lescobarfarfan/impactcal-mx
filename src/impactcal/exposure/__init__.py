"""Exposures: LitPop total-asset base (ruta B) and CNSF sumas aseguradas
disaggregated ∝ LitPop within each state (ruta A, CAL-EXP-02).
Assigns region_id (clave INEGI) and impf_* columns per state."""
