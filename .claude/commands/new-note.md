Scaffold a vault note. Ask (if not given): title, folder (`notes/theory|pipelines|reading`), one-line purpose.

1. Create `notes/<folder>/<Title_With_Underscores>.md` with: H1 title, purpose line, body stub.
2. Formatting: one paragraph = one source line; LaTeX math (`$…$`); backticks only for code/paths/IDs; Mermaid for diagrams.
3. Footer: `## Related` linking the relevant canon docs + `[[CAL_MOC]]` + `Home: [[_INDEX]]`; tags `#arm/cal #type/<theory|pipeline|reading>`.
4. Add the link under the right section of `CAL_MOC.md`.
